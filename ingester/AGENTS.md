# Ingester Agent Instructions

- Work from `/Users/ujwalgattupalli/Dev/evermind` for Python ingester commands.
- The package name is `ingester`; keep `python3 -m ingester` stable.
- The Node CLI lives in `scraper/`.
- Re-extraction should use `EVERMIND_CLI_PATH` when set, otherwise discover `scraper/bin/evermind` or `scraper/dist/src/cli.js`.
- The vault is `/Users/ujwalgattupalli/Dev/evermind/memory`.
- Do not Git-manage `memory/`; Obsidian and OCI bucket sync own persistence.

caveman mode
